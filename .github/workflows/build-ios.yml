name: Build iOS IPA

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:  # Allow manual trigger

jobs:
  build-ios:
    runs-on: macos-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup .NET 8
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.404'
        global-json-file: global.json
    
    - name: Verify .NET version
      run: |
        dotnet --version
        dotnet --list-sdks
        echo "Using SDK from global.json"
    
    - name: Install MAUI workload
      run: dotnet workload install maui
    
    - name: Restore dependencies
      run: dotnet restore AzureDeploymentsApp.csproj
    
    - name: Build iOS app (unsigned)
      run: |
        dotnet build AzureDeploymentsApp.csproj -f net8.0-ios -c Release
    
    - name: Create unsigned IPA for testing
      run: |
        # Create Payload directory
        mkdir -p Payload
        
        # Find the .app bundle (could be arm64 or x64 depending on runner)
        APP_PATH=$(find bin/Release/net8.0-ios -name "AzureDeploymentsApp.app" -type d | head -n 1)
        
        if [ -z "$APP_PATH" ]; then
          echo "❌ Error: Could not find .app bundle"
          find bin/Release/net8.0-ios -type d
          exit 1
        fi
        
        echo "📦 Found app bundle at: $APP_PATH"
        
        # Copy .app bundle to Payload
        cp -R "$APP_PATH" Payload/
        
        # Create .ipa (it's just a zip file)
        zip -r AzureDeploymentsApp-unsigned.ipa Payload
        
        echo "✅ Created unsigned IPA for testing"
        echo "⚠️  This IPA cannot be installed on real devices - signing required"
    
    # Note: To create a SIGNED IPA that can be installed on real devices and deployed via Intune,
    # you need to add code signing with your Apple Developer certificates (see steps below).
    
    # Uncomment and configure these steps when you have signing certificates:
    # - name: Import signing certificate
    #   env:
    #     CERTIFICATE_BASE64: ${{ secrets.IOS_CERTIFICATE_BASE64 }}
    #     CERTIFICATE_PASSWORD: ${{ secrets.IOS_CERTIFICATE_PASSWORD }}
    #   run: |
    #     echo $CERTIFICATE_BASE64 | base64 --decode > certificate.p12
    #     security create-keychain -p actions build.keychain
    #     security default-keychain -s build.keychain
    #     security unlock-keychain -p actions build.keychain
    #     security import certificate.p12 -k build.keychain -P $CERTIFICATE_PASSWORD -T /usr/bin/codesign
    #     security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k actions build.keychain
    
    # - name: Import provisioning profile
    #   env:
    #     PROVISIONING_PROFILE_BASE64: ${{ secrets.IOS_PROVISIONING_PROFILE_BASE64 }}
    #   run: |
    #     mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
    #     echo $PROVISIONING_PROFILE_BASE64 | base64 --decode > ~/Library/MobileDevice/Provisioning\ Profiles/profile.mobileprovision
    
    # - name: Build and create IPA
    #   run: |
    #     dotnet publish AzureDeploymentsApp.csproj \
    #       -f net8.0-ios \
    #       -c Release \
    #       /p:ArchiveOnBuild=true \
    #       /p:RuntimeIdentifier=ios-arm64 \
    #       /p:CodesignKey="iPhone Distribution" \
    #       /p:CodesignProvision="YourProvisioningProfileName"
    
    - name: Upload unsigned IPA artifact
      uses: actions/upload-artifact@v4
      with:
        name: ios-app-unsigned-ipa
        path: AzureDeploymentsApp-unsigned.ipa
        retention-days: 7
    
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ios-build
        path: bin/Release/net8.0-ios/
        retention-days: 7
    
    # When signing is configured, this will create a SIGNED IPA:
    # - name: Upload signed IPA artifact
    #   uses: actions/upload-artifact@v4
    #   with:
    #     name: ios-app-signed-ipa
    #     path: bin/Release/net8.0-ios/ios-arm64/publish/*.ipa
    #     retention-days: 30
